{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run",
            "type": "shell",
            "command": "jekyll serve --host 0.0.0.0 --port 4000",
            "runOptions": {
                "runOn": "folderOpen"
            }
        }
    ]
}
